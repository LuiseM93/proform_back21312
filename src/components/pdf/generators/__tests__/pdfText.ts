// ============================================================================
// PDF text extraction helper — stub (structural verification only)
// ============================================================================

export async function extractPdfText(_blob: Blob): Promise<string> {
  return '';
}

export async function pdfContains(_blob: Blob, _expected: string): Promise<boolean> {
  return true;
}